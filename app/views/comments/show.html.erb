<p>
  Comment by: <b><%=h @comment.user.name %></b>
  <%=h @comment.body %>
</p>

<p>
  <b>Upvotes:</b>
  <%=h @comment.num_upvotes %>
</p>

<p id="upvote">
<% if signed_in? %>
  <% if @comment.upvoted?(current_user) %>
    Upvoted
  <% else %>
    <%= link_to_remote 'Vote up', :update => "upvote",
      :url => { :action => "upvote", :id => @comment.id, :user_id => current_user.id } %>
  <% end %>
<% end %>
</p>


<%= link_to 'Edit', edit_comment_path(@comment) %> |
<%= link_to 'Back', comments_path %>
